# Nord Dark theme configuration for FZF
# This aligns the fuzzy finder perfectly with Nord Dark aesthetics.

export FZF_DEFAULT_OPTS='
  --color=fg:#d8dee9,bg:#2e3440,hl:#81a1c1
  --color=fg+:#eceff4,bg+:#3b4252,hl+:#88c0d0
  --color=info:#e5e9f0,prompt:#81a1c1,pointer:#bf616a
  --color=marker:#a3be8c,spinner:#b48ead,header:#81a1c1
  --prompt="❯ "
  --pointer="❯"
  --marker="✓"
  --reverse
  --height=45%
  --border
'

# Use fd for listing files if available
if command -v fd >/dev/null 2>&1; then
  export FZF_DEFAULT_COMMAND='fd --type f --strip-cwd-prefix --hidden --follow --exclude .git'
  export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
fi

# Use bat for file previews if available
if command -v bat >/dev/null 2>&1; then
  export FZF_CTRL_T_OPTS="--preview 'bat --color=always --style=header,grid --line-range :300 {}'"
fi
